IFS=, read -ra vals <<< $@
if [ $# != 2 ]
then
echo "Please tell the command that you want to execute  over ssh"
read input
Command=("input")
fi
for i in "${vals[@]}"
do
ssh -i /tmp/devops.pem -oStrictHostKeyChecking=no "ubuntu@$i" "$Command" < /dev/null
done

